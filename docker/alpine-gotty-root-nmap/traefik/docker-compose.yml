version: '2'

services:
  siab:
    image: hackinglab/alpine-gotty-root
    hostname: 'gotty-root'
    labels:
        - "traefik.port=8080"
        - "traefik.frontend.rule=Host:gotty-root.idocker.hacking-lab.com"
        - "traefik.protocol=http"

